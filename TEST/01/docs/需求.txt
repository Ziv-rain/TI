请基于我在\docs\参考代码文件夹中提供的 K230 官方例程结构，开发一个**红色物体实时识别程序**，完全适配K230开发板默认的3.5寸MIPI屏

#### 1. 显示配置（严格复用官方例程默认参数）
- 屏幕硬件：3.5寸MIPI液晶屏，驱动为st7701
- 固定配置参数：
  - display_mode='st7701'
  - display_size=[800, 480]（屏幕物理分辨率，宽度需做16字节对齐）
  - rgb888p_size=[1920, 1080]（摄像头采集分辨率，宽度需做16字节对齐）
- 坐标转换要求：所有识别框的坐标，必须实现从rgb888p_size采集分辨率到display_size显示分辨率的等比缩放，和例程的坐标转换逻辑完全一致。

#### 2. 代码规范与兼容性要求
- 完全遵循官方例程的代码结构、命名风格和注释规范，保证可读性和可维护性。
- 所有可调整参数（HSV阈值、面积筛选阈值、绘制框颜色/厚度），在代码开头集中定义，方便后续调试修改。
- 代码必须可直接在01Studio CanMV K230开发板上运行，无语法错误和兼容性问题。